Процедура ПМСформировать()
	//Формируем список подставных полей
	//Если ТипЗнч(Объект) <> Тип("СправочникСсылка.op_ОбъектыНедвижимости") Тогда 
	//	Агент = Объект.Агент;
	//Иначе
		//Агент = Объект.ПринадлежностьКСервиснойСлужбе;
		//Агент = ОбщийМодульОПСервер.ПолучитьТекущуюОрганизациюУСО(Объект);
		Агент = ОрганизацияУСО;
	//КонецЕсли;
	Юр = (Контрагент.ЮрФизЛицо = Перечисления.ЮрФизЛицо.ЮрЛицо);

	ПредставительАгента = ВернутьПредставительАгента();
	ДиректорДирекции = ВернутьДиректораДирекции();
	
	ЗаполнитьБлокБазовыеДанные();
	ЗаполнитьБлокДоговорИАгент(Агент, ПредставительАгента, ДиректорДирекции);
	ЗаполнитьБлокКлиентИАгент(Агент, Юр);
	ЗаполнитьБлокСтоимость();
	ЗаполнитьДляТОО_ИП_Несовершеннолетние();
	Если Объект.ГородОП.Страна = ОбщийМодульОПСервер.СлужебноеЗначениеКлиент("Страна Узбекистан") Тогда 
		ЗаполнитьБлокУзбекистан(Юр);
	КонецЕсли;	
КонецПроцедуры
